services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - database
    env_file:
      - .env

  database:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cinema
    ports:
      - "3307:3306"